---
name: release-please

on:
  workflow_dispatch:
  push:
    branches:
    - main

permissions:
  pull-requests: write
  contents: write
  packages: write

jobs:
  release-please:
    # Versioning and the changelog are owned by the upstream repository. Forks
    # don't have the HOSTINGERBOT_* secrets this job needs, and running it there
    # would bump versions that conflict with upstream on every sync, so skip it.
    if: github.repository == 'hostinger/fireactions'
    runs-on: ubuntu-latest
    steps:
    - name: Get GitHub token
      uses: actions/create-github-app-token@v1
      id: get_token
      with:
        app-id: ${{ secrets.HOSTINGERBOT_APP_ID }}
        private-key: ${{ secrets.HOSTINGERBOT_PRIVATE_KEY }}
        owner: ${{ github.repository_owner }}
        repositories: |
          fireactions

    - name: Run release-please
      uses: googleapis/release-please-action@v4
      with:
        token: ${{ steps.get_token.outputs.token }}
